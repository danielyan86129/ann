package(default_visibility = ["//visibility:public"])

cc_library(
    name = "libann",
    srcs = glob(["src/*.cpp"]),
    hdrs = glob(["src/*.h"]),
    copts = ["-std=c++98"],
    deps = ["//include:ann_include"],
)
