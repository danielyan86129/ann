package(default_visibility = ["//visibility:public"])

cc_library(
    name = "ann_include",
    hdrs = glob(["**/*.h"]),
    # include_prefix = "ANN",
    includes = ["."],
    # linkopts = linkopts,
)
